<!DOCTYPE html>
<html lang="en">

<head>
    <title>
        <%= product.productName %>
    </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/png" href="images/icons/favicon.png" />
    <link rel="stylesheet" type="text/css" href="css/util.css">
    <link rel="stylesheet" type="text/css" href="css/main.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            font-family: "Montserrat", sans-serif;
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
            box-sizing: border-box;
        }

        .breadcrumb-item+.breadcrumb-item::before {
            content: ">";
        }

        .product-image {
            width: 100%;
            max-height: 400px;
            object-fit: cover;
        }

        .rating {
            color: #f5c518;
        }

        .discount {
            color: #d9534f;
        }

        .stock-status {
            font-weight: bold;
        }

        .highlight-list {
            list-style-type: none;
            padding-left: 0;
        }

        .highlight-list li {
            margin-bottom: 10px;
        }

        .related-products img {
            width: 100%;
            height: auto;
        }

        .related-products .card {
            border: none;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .related-products .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }

        .product-page {
            margin-top: 50px;
        }

        .product-image-large {
            max-width: 400px;
            max-height: 400px;
            border: 1px solid #ddd;
            padding: 10px;
            background: #f9f9f9;
            overflow: hidden;
        }

        .product-image-large img {
            transition: transform 0.5s ease-in-out;
            /* Smooth transition on hover and zoom out */
            cursor: zoom-in;
            /* Change cursor to zoom icon on hover */
        }

        .product-image-large img:hover {
            transform: scale(1.3);
            /* Adjust the scale as needed */
            cursor: zoom-in;
            /* Change cursor to zoom-out icon when zoomed in */
        }

        .product-subimages {
            width: 80px;
            height: 80px;
            object-fit: cover;
            cursor: pointer;
            border: 1px solid #ddd;
            padding: 5px;
            background: #fff;
        }

        .product-subimages.active {
            border-color: #007bff;
        }

        .d-flex.flex-column.align-items-center {
            max-width: 100px;
        }

        .product-details h1 {
            font-size: 24px;
            font-weight: 700;
        }

        .product-price {
            font-weight: 700;
            font-size: 24px;
            color: #333;
            margin: 20px 0;
        }

        .product-offer {
            font-size: 15px;
            color: #ff0000;
            margin: 28px 5px;
        }

        .product-discount {
            font-size: 15px;
            color: #208302;
            margin: 28px 20px;
        }

        .btn-buy-now {
            background-color: #ff7f50;
            color: #fff;
            /* border: none;
            padding: 10px 20px;
            font-size: 18px;
            margin-left: 10px; */
        }

        .btn-buy-now:hover {
            background-color: #ff4500;
            color: #fff;
        }

        .out-of-stock {
            color: red;
            font-weight: bold;
        }



        .variant-box {
            width: 100%;
            max-width: 100px;
            /* Adjust the max width to make the cards smaller */
            margin: 0 auto;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .variant-box .card-body {
            padding: 0px;
            /* Reduce padding inside the card */
        }

        .variant-box .card-title {
            font-size: 0.7rem !important;
            /* Smaller font size */
            margin-bottom: 4.5px;
            /* Reduce spacing between title and text */
        }

        .variant-box .card-text {
            font-size: 0.5rem;
            /* Smaller font size */
        }

        .variant-box img {
            margin-top: 2.5px;
            /* Add a bit of spacing above the image */
            max-width: 100%;
            /* Ensure images are responsive within the card */
        }

        .variant-box img {
            margin-bottom: 6px;
        }

        .highlight {
            border: 2px solid #007bff;
            /* Highlight color (blue in this case) */
            background-color: #f0f8ff;
            /* Light background color */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
            /* Optional: Add a subtle shadow */
        }

        .variant-list a {
            color: black;
            text-decoration: none;
        }

        .quantity-control {
            display: flex;
            align-items: center;
        }

        .quantity-control input[type="number"] {
            width: 50px;
            text-align: center;
            border: none;
            outline: none;
        }

        .quantity-control button {
            padding: 0 10px;
            background-color: #ddd;
            border: 1px solid #ccc;
            cursor: pointer;
            font-size: 18px;
            outline: none;
        }

        .quantity-control button:disabled {
            cursor: not-allowed;
            background-color: #f9f9f9;
        }

        .quantity-control button:hover:not(:disabled) {
            background-color: #ccc;
        }
    </style>

</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">CellCity</a>
        <!-- Add other navbar content here -->
    </nav>

    <!-- Breadcrumbs -->
    <nav aria-label="breadcrumb" class="container mt-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item"><a href="/shop">Shop</a></li>
            <li class="breadcrumb-item active" aria-current="page">
                <%= product.productName %>
            </li>
        </ol>
    </nav>

    <div class="container product-page">
        <div class="row">
            <!-- Product Images -->
            <div class="col-md-6 d-flex">
                <!-- Subimages on the left -->
                <div class="d-flex flex-column align-items-center mr-3">
                    <% if (product.variants.length> 0 && variant.variantImages && variant.variantImages.length > 0) { %>
                        <% variant.variantImages.forEach((image, index)=> { %>
                            <img src="<%= image.replace('uploads', '') %>" alt="<%= product.productName %>"
                                class="img-fluid product-subimages mb-2" data-src="<%= image.replace('uploads', '') %>">
                            <% }); %>
                                <% } else { %>
                                    <img src="/path/to/default-image.png" alt="Default Image">
                                    <% } %>
                </div>

                <!-- Large image on the right -->
                <div class="product-image-large">
                    <% if (product.variants.length> 0 && variant.variantImages && variant.variantImages.length > 0) { %>
                        <img id="largeImage" src="<%= variant.variantImages[0].replace('uploads', '') %>"
                            alt="<%= product.productName %>" class="img-fluid">
                        <% } else { %>
                            <img id="largeImage" src="/path/to/default-image.png" alt="Default Image" class="img-fluid">
                            <% } %>
                </div>
            </div>


            <!-- Product Details -->

            <div class="col-md-6 product-details">
                <h1>
                    <%= product.productName %>
                </h1>
                <div class="rating">
                    <% for (var i=0; i < 5; i++) { %>
                        <i class="fa <%= i < product.rating ? 'fa-star' : 'fa-star-o' %>"></i>
                        <% } %>
                </div>
                <div class="d-flex">
                    <% if (product.variants.length> 0) { %>
                        <p class="product-price">₹<%= variant.price || 'N/A' %>
                        </p>
                        <% const discount=variant.discount || 0; %>
                            <p class="product-discount">
                                <%= discount %>%
                            </p>
                            <p class="product-offer"><s>₹<%= variant.price + discount %></s></p>
                            <% } else { %>
                                <p class="product-price">Price Not Available</p>
                                <% } %>
                </div>

                <p class="<%= product.variants.length > 0 && variant.stocks > 0 ? 'text-success' : 'out-of-stock' %>">
                    <%= product.variants.length> 0 && variant.stocks > 0 ? 'In Stock' : 'Out of Stock' %>
                </p>
                <form action="/cart/add" method="POST">
                    <!-- <div class="quantity-control">
                        <button type="button" id="increase">+</button>
                        <input type="number" name="quantity" id="quantity" value="1" min="1" required>
                        <button type="button" id="decrease">-</button>
                    </div> -->
                    <input type="hidden" name="variantId" value="<%= variant._id %>">

                    <button class="btn btn-primary" <%=product.variants.length> 0 && variant.stocks > 0 ? '' :
                        'disabled' %>>
                        Add to Cart
                    </button>
                    <% if (messages.info) { %>
                        <div class="alert alert-info">
                            <%= messages.info %>
                        </div>
                        <% } %>
                            <% if (messages.success) { %>
                                <div class="alert alert-success">
                                    <%= messages.success %>
                                </div>
                                <% } %>
                                    <% if (messages.error) { %>
                                        <div class="alert alert-danger">
                                            <%= messages.error %>
                                        </div>
                                        <% } %>

                </form>

                <a href="/cart"><button class="btn btn-buy-now" <%=product.variants.length> 0 &&
                        variant.stocks > 0 ? '' : 'disabled' %>>Show Cart</button></a>

                <ul class="highlight-list">
                    <% if (product.highlights) { %>
                        <% product.highlights.split('\n').forEach(function(highlight) { %>
                            <li>
                                <%= highlight %>
                            </li>
                            <% }); %>
                                <% } %>
                </ul>

                <div class="variant-info">
                    <div class="variant-details">
                        <h5>
                            <%= variant.color || 'Unknown Color' %> - <%= variant.storage || 'Unknown Storage' %> /
                                    <%= variant.RAM || 'Unknown RAM' %>
                        </h5>
                        <p>Price: ₹<%= variant.price || 'N/A' %>
                        </p>
                        <p>Stock: <%= variant.stocks || 'N/A' %>
                        </p>
                        <div class="container variant-list">
                            <h3>Available Variants:</h3>
                            <div class="row">
                                <% product.variants.forEach(function(currentVariant) { %>
                                    <div class="col-md-3 col-sm-3">
                                        <a href="/shop/product/<%= currentVariant._id %>" class="variant-link">
                                            <div class="card variant-box mb-3 
                                        <%= currentVariant._id.equals(variant._id) ? 'highlight' : '' %>">
                                                <div class="card-body p-2">
                                                    <% if (currentVariant.variantImages &&
                                                        currentVariant.variantImages.length> 0) { %>
                                                        <img src="<%= currentVariant.variantImages[0].replace('uploads', '') %>"
                                                            alt="<%= currentVariant.color %> Variant" class="img-fluid">
                                                        <% } else { %>
                                                            <img src="/path/to/default-image.png"
                                                                alt="Default Variant Image" class="img-fluid">
                                                            <% } %>
                                                                <h5 class="card-title" style="font-size: 0.8rem;">
                                                                    <%= currentVariant.storage || 'Unknown Storage' %> /
                                                                        <%= currentVariant.RAM || 'Unknown RAM' %>
                                                                </h5>
                                                                <p class="card-text mb-1" style="font-size: 0.6rem;">
                                                                    Price:
                                                                    ₹<%= currentVariant.price || 'N/A' %>
                                                                </p>
                                                                <p class="card-text mb-1" style="font-size: 0.6rem;">
                                                                    Color:
                                                                    <%= currentVariant.color || 'N/A' %>
                                                                </p>
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                    <% }); %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>





    <!-- Footer -->
    <footer class="footer mt-5">
        <div class="container text-center">
            <p>&copy; 2024 E-Commerce Site. All Rights Reserved.</p>
            <p>Follow us on
                <a href="#">Facebook</a>,
                <a href="#">Twitter</a>,
                <a href="#">Instagram</a>
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS and dependencies -->
    <!-- <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script> -->
    <!-- Ensure only one jQuery is included -->
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

<!-- Bootstrap JS (Match this with your Bootstrap CSS version) -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<!-- elevateZoom (Load after jQuery) -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/elevatezoom/3.0.8/jquery.elevatezoom.min.js"></script>

<!-- Your custom script -->
<script>
    document.addEventListener('DOMContentLoaded', function () {
        document.querySelectorAll('.product-subimages').forEach(function (thumbnail) {
            thumbnail.addEventListener('click', function () {
                // Remove the 'active' class from all subimages
                document.querySelectorAll('.product-subimages').forEach(function (img) {
                    img.classList.remove('active');
                });

                // Add 'active' class to the clicked subimage
                this.classList.add('active');

                // Change the source of the large image
                const newSrc = this.getAttribute('data-src');
                console.log('New Source:', newSrc);
                document.getElementById('largeImage').setAttribute('src', newSrc);

                // // Update the elevateZoom with the new image
                // $('#largeImage').data('zoomImage', newSrc).elevateZoom({
                //     zoomType: "lens",
                //     lensShape: "round",
                //     lensSize: 200,
                //     borderSize: 1,
                //     borderColour: "#ddd"
                // });
            });
        });
    });
</script>


</html>